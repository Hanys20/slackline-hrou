{
  "name": "slackline-hrou",
  "private": true,
  "version": "1.0.0",
  "description": "Web projekt pro Slackline Hrou – nasazovaný jako statický web přes Cloudflare Workers (Static Assets).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
